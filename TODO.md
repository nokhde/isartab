Prio:
- Bug: MagicFill-Fehler verursacht verschwinden
- Möglichkeit, alte Sachen zu löschen / autodelete
- The deadline is advisory only

Less Prio:
- Datenschutzerklärung modular
- Bot/Spam Protection
- Protection gegen Manipulation der Forms
- Admin-only für Events
- experience-level besser zeigen
- Duplication Warn
- A registration deadline can never be cleared
- "Propose Rooms" fails silently
- /waiting is described as an SW-cached shell in pages.py but is neither precached nor intercepted by sw.js, so it has no offline support at all
- /static/vendor/tailwind.js is not in the SW precache, so offline styling only survives as long as the browser's heuristic HTTP cache keeps it